<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>How many Czech brand templates are there? Three.</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<style>{{ inline_css }}</style>
</head>
<body>
<main>

<header class="masthead">
    <p class="eyebrow">Brand Reflection Series &middot; Episode 2 &middot; companion to
        <a href="https://sandovabarbora.github.io/color-fingerprint-cz/" style="color:inherit;">Color fingerprint</a>
        &amp;
        <a href="https://sandovabarbora.github.io/sound-fingerprint-cz/" style="color:inherit;">Sound fingerprint</a>
    </p>
    <h1>Three templates, one institution.</h1>
    <p class="lede">What happens when an AI reads brand homepages instead of brand briefs? For {{ counts.n_brands_used }} Czech brands, it draws a map where the boundaries don&apos;t match the sectors. This report shows that map, walks one brand (Air Bank) through what it means for their strategy, and gives you a tool to generate the same brief for any brand on it.</p>
    <p class="bio">Data &amp; cloud engineer at Alma Career. Built across a few weekends to test what brand competitive analysis looks like when it starts from public data instead of from pitch decks. Part of a five-piece <a href="https://sandovabarbora.github.io/brand-reflection-cz/">Brand Reflection Series</a>.</p>
    <p class="byline">By {{ author }}. <a href="{{ repo_url }}">Source on GitHub</a>.</p>
</header>

<section class="hero-stat">
    <p class="hero-stat-line">Eight brands. Four sectors. One homepage template.</p>
    <p class="hero-stat-cap">&#268;SOB, &#268;esk&aacute; spo&#345;itelna, Fio banka, Air Bank, Komer&#269;n&iacute; banka, Generali, &#268;EZ, &#352;koda Auto. The boundary between them, on the homepage layer, is zero.</p>
</section>

<section class="method">
    <p class="method-label">How it works</p>
    <p class="method-body">An AI language model reads each Czech brand&apos;s homepage and plots the brand on a 2D map (filled circles, coloured by neighborhood). Brands whose homepages read alike land near each other; brands that read differently land apart. The same model reads top rising Czech queries from Google Trends and drops them onto the same map (outlined diamonds, sized by how fast each query is rising), so you can see which empty zones a rising query lands in. Full technical breakdown in the appendix.</p>
</section>

<section>
    <div class="section-label"><span class="num">01</span><h2>The map</h2></div>
    <p class="section-intro">Brands are filled circles, colored by neighborhood. Rising queries are outlined diamonds, sized by how fast each query is rising. Distance on the map approximates how differently the brand homepages read to the AI &mdash; brands close together share homepage language; brands far apart don&apos;t. Neighborhood names come from the dominant sector in each group, not from me.</p>
    <div class="map-wrap">
        <figure class="map-frame">
            {{ map_svg | safe }}
            <figcaption class="scatter-cap">Click any neighborhood to inspect the brands and vocabulary that define it. The dashed red arrow shows Air Bank&apos;s recommended trajectory &mdash; from inside the Banking + heavy industry neighborhood toward the edge of mass-market consumer (the position held by Pilulka and Avast).</figcaption>
        </figure>
        <aside id="cluster-panel" class="cluster-panel" aria-live="polite">
            <p class="cluster-panel-hint">Click a neighborhood on the map to see which brands share it and which words define it.</p>
        </aside>
    </div>
    <div class="legend-strip">
{% for chip in legend %}
        <div class="legend-chip">
            <span class="legend-dot" style="background: {{ chip.color }};"></span>
            <div class="legend-meta">
                <p class="legend-name">{{ chip.name }}</p>
                <p class="legend-brands">{{ chip.n_brands }} brands &mdash; {{ chip.brand_examples }}</p>
            </div>
        </div>
{% endfor %}
        <div class="legend-chip">
            <svg class="legend-diamond" viewBox="0 0 14 14" aria-hidden="true">
                <polygon points="7,1 13,7 7,13 1,7" fill="none" stroke="#181818" stroke-width="1.4"/>
            </svg>
            <div class="legend-meta">
                <p class="legend-name">Rising Czech query</p>
                <p class="legend-brands">{{ counts.n_trends }} trends &mdash; sized by how fast each is rising</p>
            </div>
        </div>
    </div>
</section>

<aside class="pull">
    <p class="pull-q">Czech banks, &#268;EZ and &#352;koda Auto cluster as <span class="num">one</span> institutional brand. Your brand is not where you think it is.</p>
    <p class="pull-cap">The boundary between &#268;SOB and &#268;EZ is invisible at the homepage layer. The boundary between Lidl and Avast is invisible at the homepage layer. The boundaries we name in pitch decks are campaign-level fictions that the homepage doesn't honor.</p>
</aside>

<section>
    <div class="section-label"><span class="num">02</span><h2>One brand, end to end: Air Bank</h2></div>
    <p class="section-intro">If the map is the diagnostic, this is what doing something with it looks like. The brand is Air Bank; the moves stay the same for any brand on the map. Four steps from &quot;where am I&quot; to &quot;what slides go in the deck.&quot;</p>

    <div class="worked">
        <article class="worked-step">
            <p class="worked-num">01</p>
            <div>
                <h3>Where Air Bank sits on the map</h3>
                <p>Air Bank embeds inside the <strong>Banking + heavy industry</strong> neighborhood, next to &#268;SOB, &#268;esk&aacute; spo&#345;itelna, Fio banka, Komer&#269;n&iacute; banka, Generali, &#268;EZ, and &#352;koda Auto. Three banks plus an insurer plus an energy company plus a carmaker all read as one brand to the homepage layer.</p>
                <p class="worked-note">On the map, Air Bank tilts slightly toward the consumer side of the institutional neighborhood &mdash; closer to the boundary than its peers. That tilt is the only visible difference between Air Bank and Komer&#269;n&iacute;, and it&apos;s small.</p>
            </div>
        </article>

        <article class="worked-step">
            <p class="worked-num">02</p>
            <div>
                <h3>The vocabulary it shares with seven other brands</h3>
                <p>The words that distinguish the institutional neighborhood from the other three on the map, taken directly from how those homepages read, are: <code>banka</code>, <code>&scaron;koda</code>, <code>&#269;ez</code>, <code>fio</code>, <code>&#269;esk&aacute;</code>.</p>
                <p>If Air Bank&apos;s homepage shares those tokens with &#268;EZ&apos;s homepage, then to a buyer&apos;s first read, Air Bank <em>is</em> &#268;EZ. The challenger positioning &mdash; &quot;the bank that&apos;s different from Komer&#269;n&iacute;&quot; &mdash; is decided in the campaign layer and quietly undone in the homepage layer.</p>
            </div>
        </article>

        <article class="worked-step">
            <p class="worked-num">03</p>
            <div>
                <h3>Which playbook moves apply, in order</h3>
                <p><strong>Move 02</strong> &mdash; list the institutional vocabulary explicitly in the brief and rule it out. Not as creative aspiration; as a word-level constraint. The first draft that lands with <code>banka</code>, <code>partnerem</code> or <code>silnej&scaron;&iacute;</code> on the homepage goes back.</p>
                <p><strong>Move 03</strong> &mdash; stop pitching against named banks. The boundary that matters is the one between the institutional neighborhood and the mass-market consumer neighborhood. The target isn&apos;t the center of mass-market (Lidl, Albert) &mdash; it&apos;s the <em>edge</em> closest to Air Bank&apos;s current position, held by brands like Pilulka and Avast that read as consumer without reading as supermarket. That edge is the strategic ask.</p>
                <p><strong>Move 01</strong> &mdash; after the rebrand, re-score. If the homepage didn&apos;t move neighborhoods, neither did the brand. Soft success at the campaign layer with no movement at the homepage layer is the failure mode this episode is built to catch.</p>
            </div>
        </article>

        <article class="worked-step">
            <p class="worked-num">04</p>
            <div>
                <h3>The brief this produces</h3>
                <p>Below is the literal one-page brief the diagnostic outputs. Not a description of what the brief would say &mdash; the brief itself, ready to hand to a copywriter on Monday. Substitute any brand on the map and the layout stays the same; only the token lists and the trajectory change.</p>

                <aside class="brief-artifact" aria-label="Air Bank homepage repositioning brief">
                    <header class="brief-head">
                        <p class="brief-eyebrow">HOMEPAGE REPOSITIONING BRIEF &middot; 1 PAGE</p>
                        <p class="brief-title">Air Bank</p>
                    </header>

                    <div class="brief-grid">
                        <div class="brief-cell">
                            <p class="brief-label">Where you are now</p>
                            <p class="brief-value">Banking + heavy industry neighborhood</p>
                            <p class="brief-meta">Sharing your homepage template with: &#268;SOB, &#268;esk&aacute; spo&#345;itelna, Fio banka, Komer&#269;n&iacute; banka, Generali, &#268;EZ, &#352;koda Auto</p>
                        </div>
                        <div class="brief-cell">
                            <p class="brief-label">Where you need to go</p>
                            <p class="brief-value">Edge of mass-market consumer</p>
                            <p class="brief-meta">Realistic target neighbors: Pilulka, Avast. Aspirational direction, not destination: Albert, Rohl&iacute;k, Seznam.cz. Not the goal: Lidl, McDonald&apos;s.</p>
                        </div>
                    </div>

                    <div class="brief-grid">
                        <div class="brief-cell">
                            <p class="brief-label brief-label-warn">DROP from your homepage</p>
                            <ul class="brief-tokens">
                                <li>banka</li>
                                <li>&scaron;koda</li>
                                <li>&#269;ez</li>
                                <li>fio</li>
                                <li>&#269;esk&aacute;</li>
                                <li>partnerem</li>
                                <li>silnej&scaron;&iacute;</li>
                            </ul>
                            <p class="brief-meta">The top five are the words that distinguish the institutional neighborhood from the others. The last two are vocabulary the playbook flags as institutional&#8209;reinforcing.</p>
                        </div>
                        <div class="brief-cell">
                            <p class="brief-label brief-label-good">ADOPT on your homepage</p>
                            <ul class="brief-tokens brief-tokens-good">
                                <li>akce</li>
                                <li>novinky</li>
                                <li>aktu&aacute;ln&iacute; nab&iacute;dka</li>
                                <li>online</li>
                                <li>product-led layout</li>
                            </ul>
                            <p class="brief-meta">Don&apos;t become a supermarket. The target is the <em>edge</em> of the mass-market neighborhood &mdash; the position held by Pilulka and Avast, which sit in mass-market without being promo-driven. A bank can occupy that edge; it cannot occupy a Lidl.</p>
                        </div>
                    </div>

                    <div class="brief-success">
                        <p class="brief-label">Success metric</p>
                        <p class="brief-success-body">Re-embed the homepage 90 days post-launch. The Air Bank circle must move off the institutional centroid and toward the boundary &mdash; the target is the edge between institutional and consumer, where Pilulka sits. Full mass-market migration is not the goal; meaningful drift is. If the circle hasn&apos;t moved at all, the brand didn&apos;t move &mdash; the campaign moved.</p>
                    </div>

                    <footer class="brief-foot">
                        <p>Generated from the strategic topography map. Same template for any brand in the set; swap the brand name and the word lists update from the words that distinguish each neighborhood from the others.</p>
                    </footer>
                </aside>
            </div>
        </article>
    </div>
</section>

<section>
    <div class="section-label"><span class="num">03</span><h2>Generate the same brief for any brand</h2></div>
    <p class="section-intro">The Air Bank brief above is one application of the diagnostic. This generator runs the same logic for any brand in the set. The &quot;drop&quot; and &quot;adopt&quot; word lists come from the words that distinguish each neighborhood from the others; the &quot;where the map points next&quot; targets come from the brand&apos;s nearest neighboring neighborhood and the brands in it ranked by distance.</p>

    <p class="generator-caveat"><strong>The generator is mechanical.</strong> Geometrically nearest does not mean strategically right. Pilsner Urquell&apos;s nearest neighborhood is mass-market consumer, but recommending a premium beer drift toward Lidl is obviously wrong. Use the output as a structured starting point for a strategist&apos;s judgment, not as a directive. The Air Bank brief above shows what the human edit looks like.</p>

    <div class="generator-controls">
        <label for="brief-brand-picker" class="generator-label">Brand</label>
        <select id="brief-brand-picker" class="generator-select">
            <option value="">&mdash; pick a brand &mdash;</option>
        </select>
    </div>

    <div id="generated-brief" class="generated-brief"></div>
</section>

<section>
    <div class="section-label"><span class="num">04</span><h2>Appendix</h2></div>
    <p class="section-intro">The reference material behind the diagnostic. Open whichever you need; the report above stands without them.</p>

    <details class="appendix">
        <summary>Four patterns the map reveals</summary>
        <div class="appendix-body">
            <p class="appendix-lede">Each pattern is a finding that the map made visible. Patterns explain <em>why</em> the four neighborhoods matter; the Air Bank walk-through shows how to act on them.</p>
            <div class="patterns">
{% for p in patterns %}
                <article class="pattern">
                    <p class="num">{{ "%02d" | format(loop.index) }}</p>
                    <div>
                        <h3>{{ p.title }}</h3>
                        <p class="evidence">{{ p.evidence }}</p>
                        <p class="takeaway"><strong>Why it matters</strong>{{ p.takeaway }}</p>
                    </div>
                </article>
{% endfor %}
            </div>
        </div>
    </details>

    <details class="appendix">
        <summary>Full playbook (five moves)</summary>
        <div class="appendix-body">
            <p class="appendix-lede">Five moves a strategist or creative director can run on next week&apos;s pitch. Moves 01&ndash;03 already show up in the Air Bank walk-through; 04 and 05 cover trends and re-scoring.</p>
            <div class="playbook">
{% for move in playbook %}
                <article class="play">
                    <p class="play-num">{{ "%02d" | format(loop.index) }}</p>
                    <div>
                        <h3>{{ move.title }}</h3>
                        <p class="play-body">{{ move.body }}</p>
                    </div>
                </article>
{% endfor %}
            </div>
        </div>
    </details>

    <details class="appendix">
        <summary>What this is and isn&apos;t</summary>
        <div class="appendix-body">
            <p class="appendix-lede">A few honest things to know before you act on the report or commission the diagnostic for a client.</p>

            <ul class="limits-list">
                <li>
                    <strong>The 24-brand demo is a method test, not a Czech market census.</strong> When you commission this for your client, we agree the brand set on the call (typically the client plus 3&ndash;6 competitors, plus a few reference brands). The public demo shows what the method does; a client engagement runs it on your actual competitive set.
                </li>
                <li>
                    <strong>The map groups brands by how their homepage text reads, not by what sector they&apos;re &quot;supposed&quot; to be in.</strong> Two brands near each other use similar homepage language and structure; two brands far apart don&apos;t. That&apos;s why a bank, an insurance company, an energy company and a carmaker can land together &mdash; the homepage layer doesn&apos;t care about your sector classification.
                </li>
                <li>
                    <strong>The top words in each neighborhood are descriptive, not prescriptive.</strong> They show what register a neighborhood shares (some are inevitably brand names, since a cluster of banks will have &quot;banka&quot; in its signature). Read them as &quot;the language this neighborhood already speaks,&quot; not as &quot;ship these specific words.&quot;
                </li>
                <li>
                    <strong>Homepages are one signal, not the whole brand.</strong> A brand can play very differently on TV, OOH or social and still get classified by its homepage snippet here. The map describes &quot;how the homepage reads.&quot; If you want to score how a specific spot lands, that&apos;s the <a href="https://sandovabarbora.github.io/spot-scorer/">Spot Scorer</a>, not this report.
                </li>
                <li>
                    <strong>The brief generator is mechanical and sometimes wrong.</strong> It points Pilsner Urquell toward Lidl, which is obviously not the move for a premium beer. Use the auto-generated brief as a structured first draft; the strategist&apos;s edit is where the judgment lives. The Air Bank brief above shows what that edit looks like in practice.
                </li>
            </ul>
        </div>
    </details>

    <details class="appendix">
        <summary>How it was built (technical specifics)</summary>
        <div class="appendix-body">
            <p class="appendix-lede">For the reader who wants the AI / ML pipeline named.</p>
            <p>Each Czech brand homepage (title, meta description, first headings, first non-promo paragraphs, capped at 600 characters per brand) is read by a multilingual AI language model &mdash; <code>intfloat/multilingual-e5-base</code> &mdash; which turns the text into a 768-dimensional vector capturing meaning across languages. The same model reads the top rising Czech queries (pytrends + hand-curated fallback). The vectors are projected to 2D for the map via UMAP and grouped into neighborhoods via KMeans (run on the original 768-dim AI vectors, not on the 2D projection). Neighborhood labels are rolled from sector composition. The opportunity score for each trend is velocity &times; (1 &minus; mean similarity to the three closest brands in the AI vector space) &mdash; a high score means the trend lives in an empty zone of the map.</p>
            <p>This is a text-mode AI pipeline. The sister projects in the series use vision AI (Color Fingerprint) and audio AI (Sound Fingerprint); the Spot Scorer combines all three into one multimodal score for any video URL.</p>
        </div>
    </details>

    <details class="appendix">
        <summary>Rising trends and where they fall on the map</summary>
        <div class="appendix-body">
            <p class="appendix-lede">For each rising Czech query, the three brands whose homepage reads most similarly. Rows flagged <span class="low-conf-inline">low&#8209;signal</span> mean the top three brand matches are essentially tied &mdash; the &quot;winner&quot; is arbitrary, which is itself a finding (a sector gap in the brand set).</p>
            <table class="opp-table">
                <thead>
                    <tr>
                        <th>Trend</th>
                        <th class="num-col">Velocity</th>
                        <th>Nearest brands</th>
                        <th class="num-col">Saturation</th>
                    </tr>
                </thead>
                <tbody>
{% for o in opportunities %}
                    <tr class="{% if o.low_confidence %}opp-low-conf{% endif %}">
                        <td class="opp-trend">
                            {{ o.trend }}
                            {% if o.low_confidence %}<span class="low-conf-badge">low signal</span>{% endif %}
                        </td>
                        <td class="num-col">{{ "%.0f" | format(o.velocity) }}</td>
                        <td class="opp-nearby">
                            {{ o.near_brand_1 }}<span class="opp-sim">({{ "%.2f" | format(o.near_brand_1_sim) }})</span>
                            {% if o.near_brand_2 %} &middot; {{ o.near_brand_2 }}<span class="opp-sim">({{ "%.2f" | format(o.near_brand_2_sim) }})</span>{% endif %}
                            {% if o.near_brand_3 %} &middot; {{ o.near_brand_3 }}<span class="opp-sim">({{ "%.2f" | format(o.near_brand_3_sim) }})</span>{% endif %}
                        </td>
                        <td class="num-col">{{ "%.2f" | format(o.saturation_score) }}</td>
                    </tr>
{% endfor %}
                </tbody>
            </table>
        </div>
    </details>
</section>

<footer>
    <div>
        <h3>Brand Reflection Series</h3>
        <p><a href="https://sandovabarbora.github.io/color-fingerprint-cz/">Episode 1 &mdash; Color fingerprint</a></p>
        <p><a href="https://sandovabarbora.github.io/sound-fingerprint-cz/">Episode 1.5 &mdash; Sound fingerprint</a></p>
        <p>Episode 2 (this) &mdash; Strategic topography</p>
        <p><a href="https://sandovabarbora.github.io/brand-reflection-cz/">Series hub + case studies</a></p>
        <p><a href="https://sandovabarbora.github.io/spot-scorer/">Spot Scorer &mdash; instant brief generator</a></p>
    </div>
    <div>
        <h3>Code</h3>
        <p><a href="{{ repo_url }}">strategic-topography-cz</a></p>
        <p>MIT for code, CC&#8209;BY&#8209;4.0 for derived data.</p>
    </div>
    <div>
        <h3>Disclaimer</h3>
        <p>Analysis of publicly available brand communication. Brands are referenced for analytical purposes only. Positioning reflects current homepage messaging at time of scrape. No agency attribution is implied.</p>
    </div>
</footer>

</main>

<script id="brief-data" type="application/json">{{ brief_data_json | safe }}</script>
<script>
(function() {
    const dataEl = document.getElementById("brief-data");
    const picker = document.getElementById("brief-brand-picker");
    const out = document.getElementById("generated-brief");
    if (!dataEl || !picker || !out) return;
    const briefs = JSON.parse(dataEl.textContent);
    // Group brands by their current neighborhood for the picker. A strategist
    // scanning the dropdown reads the neighborhood structure (banks here,
    // mass-market there) before they pick a brand. Alphabetical-only flat
    // list hides the most useful piece of metadata.
    const byNeighborhood = {};
    Object.values(briefs).forEach(b => {
        if (!byNeighborhood[b.current_cluster]) byNeighborhood[b.current_cluster] = [];
        byNeighborhood[b.current_cluster].push(b.brand);
    });
    Object.keys(byNeighborhood).sort().forEach(n => {
        const group = document.createElement("optgroup");
        group.label = n;
        byNeighborhood[n].sort().forEach(b => {
            const opt = document.createElement("option");
            opt.value = b;
            opt.textContent = b;
            group.appendChild(opt);
        });
        picker.appendChild(group);
    });

    function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
    function el(tag, opts) {
        const node = document.createElement(tag);
        if (opts && opts.cls) node.className = opts.cls;
        if (opts && opts.text != null) node.textContent = opts.text;
        return node;
    }
    function tokenList(tokens, goodClass) {
        const ul = el("ul", { cls: goodClass ? "brief-tokens brief-tokens-good" : "brief-tokens" });
        tokens.forEach(t => {
            const li = el("li");
            li.textContent = t;
            ul.appendChild(li);
        });
        return ul;
    }

    function renderBrief(brand) {
        clear(out);
        const b = briefs[brand];
        if (!b) return;
        const card = el("aside", { cls: "brief-artifact" });
        card.setAttribute("aria-label", brand + " homepage repositioning brief");

        const head = el("header", { cls: "brief-head" });
        head.appendChild(el("p", { cls: "brief-eyebrow", text: "HOMEPAGE REPOSITIONING BRIEF · 1 PAGE" }));
        head.appendChild(el("p", { cls: "brief-title", text: b.brand }));
        card.appendChild(head);

        // Where you are / Where you need to go
        const row1 = el("div", { cls: "brief-grid" });
        const c1 = el("div", { cls: "brief-cell" });
        c1.appendChild(el("p", { cls: "brief-label", text: "Where you are now" }));
        c1.appendChild(el("p", { cls: "brief-value", text: b.current_cluster + " neighborhood" }));
        const peersText = b.peers.length
            ? "Sharing your homepage template with: " + b.peers.join(", ")
            : "No other brand currently sits in this neighborhood.";
        c1.appendChild(el("p", { cls: "brief-meta", text: peersText }));
        row1.appendChild(c1);

        const c2 = el("div", { cls: "brief-cell" });
        c2.appendChild(el("p", { cls: "brief-label", text: "Where the map points next" }));
        c2.appendChild(el("p", { cls: "brief-value", text: "Edge of " + b.target_cluster.toLowerCase() }));
        const targetParts = [];
        if (b.realistic.length) targetParts.push("Realistic target neighbors: " + b.realistic.join(", "));
        if (b.aspirational.length) targetParts.push("Aspirational direction, not destination: " + b.aspirational.join(", "));
        if (b.avoid.length) targetParts.push("Not the goal: " + b.avoid.join(", "));
        c2.appendChild(el("p", { cls: "brief-meta", text: targetParts.join(". ") + "." }));
        row1.appendChild(c2);
        card.appendChild(row1);

        // Drop / Adopt
        const row2 = el("div", { cls: "brief-grid" });
        const d1 = el("div", { cls: "brief-cell" });
        d1.appendChild(el("p", { cls: "brief-label brief-label-warn", text: "DROP from your homepage" }));
        d1.appendChild(tokenList(b.drop_tokens, false));
        d1.appendChild(el("p", { cls: "brief-meta", text: "Words that distinguish your current neighborhood from the others on the map. Some are inevitably brand names — that's expected (a neighborhood of banks will have 'banka' in it). Treat the rest as the institutional or categorical vocabulary you're carrying." }));
        row2.appendChild(d1);

        const d2 = el("div", { cls: "brief-cell" });
        d2.appendChild(el("p", { cls: "brief-label brief-label-good", text: "ADOPT on your homepage" }));
        d2.appendChild(tokenList(b.adopt_tokens, true));
        d2.appendChild(el("p", { cls: "brief-meta", text: "Words that distinguish your nearest neighboring neighborhood from yours. The target isn't the center of that neighborhood — it's the edge near " + (b.realistic[0] || "your nearest peer") + ", which sits in the target neighborhood without becoming its loudest archetype." }));
        row2.appendChild(d2);
        card.appendChild(row2);

        // Success metric
        const succ = el("div", { cls: "brief-success" });
        succ.appendChild(el("p", { cls: "brief-label", text: "Success metric" }));
        const succBody = "Re-read the homepage with the AI 90 days post-launch. The " + b.brand + " circle must move off its current position inside " + b.current_cluster + " and toward the boundary — the target is the edge between " + b.current_cluster + " and " + b.target_cluster + ", where " + (b.realistic.join(" and ") || "the nearest edge brands") + " sits. Meaningful drift, not full migration. If the circle hasn't moved at all, the brand didn't move — the campaign moved.";
        succ.appendChild(el("p", { cls: "brief-success-body", text: succBody }));
        card.appendChild(succ);

        const foot = el("footer", { cls: "brief-foot" });
        foot.appendChild(el("p", { text: "Generated from the strategic topography map. Word lists come from the words that distinguish each neighborhood from the others; trajectory targets come from each brand's nearest neighboring neighborhood and the brands in it ranked by distance. Read alongside the 'What this is and isn't' block in the appendix." }));
        card.appendChild(foot);

        out.appendChild(card);
    }

    picker.addEventListener("change", e => {
        if (e.target.value) renderBrief(e.target.value);
        else clear(out);
    });
})();
</script>

<script id="cluster-panel-data" type="application/json">{{ cluster_panel_json | safe }}</script>
<script>
(function() {
    const dataEl = document.getElementById("cluster-panel-data");
    if (!dataEl) return;
    const clusters = JSON.parse(dataEl.textContent);
    const byId = new Map(clusters.map(c => [String(c.id), c]));
    const panel = document.getElementById("cluster-panel");
    const map = document.querySelector(".topo-map");
    if (!panel || !map) return;

    const groups = Array.from(map.querySelectorAll(".cluster-group"));

    function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
    function el(tag, opts) {
        const node = document.createElement(tag);
        if (opts && opts.cls) node.className = opts.cls;
        if (opts && opts.text != null) node.textContent = opts.text;
        return node;
    }

    function renderPanel(c) {
        clear(panel);
        const header = el("div", { cls: "cluster-panel-head" });
        const dot = el("span", { cls: "cluster-panel-dot" });
        dot.style.background = c.color;
        header.appendChild(dot);
        header.appendChild(el("p", { cls: "cluster-panel-name", text: c.name }));
        panel.appendChild(header);

        panel.appendChild(el("p", { cls: "cluster-panel-meta", text: c.brands.length + " brands in this neighborhood" }));

        const brandsLabel = el("p", { cls: "cluster-panel-label", text: "Brands" });
        panel.appendChild(brandsLabel);
        const brandList = el("p", { cls: "cluster-panel-brands", text: c.brands.join(", ") });
        panel.appendChild(brandList);

        const tokensLabel = el("p", { cls: "cluster-panel-label", text: "Words that define this neighborhood" });
        panel.appendChild(tokensLabel);
        const tokens = el("ul", { cls: "cluster-panel-tokens" });
        c.top_tokens.forEach(tok => {
            const li = el("li", { cls: "cluster-panel-token", text: tok });
            tokens.appendChild(li);
        });
        panel.appendChild(tokens);

        const closeBtn = el("button", { cls: "cluster-panel-close", text: "Clear selection" });
        closeBtn.type = "button";
        closeBtn.addEventListener("click", clearSelection);
        panel.appendChild(closeBtn);
    }

    function selectCluster(cid) {
        const c = byId.get(String(cid));
        if (!c) return;
        groups.forEach(g => {
            const active = g.getAttribute("data-cluster-id") === String(cid);
            g.classList.toggle("is-active", active);
            g.classList.toggle("is-dimmed", !active);
        });
        renderPanel(c);
    }

    function clearSelection() {
        groups.forEach(g => {
            g.classList.remove("is-active");
            g.classList.remove("is-dimmed");
        });
        clear(panel);
        panel.appendChild(el("p", { cls: "cluster-panel-hint", text: "Click a neighborhood on the map to see which brands share it and which words define it." }));
    }

    groups.forEach(g => {
        const cid = g.getAttribute("data-cluster-id");
        g.addEventListener("click", () => selectCluster(cid));
        g.addEventListener("keydown", (e) => {
            if (e.key === "Enter" || e.key === " ") {
                e.preventDefault();
                selectCluster(cid);
            }
        });
    });
})();
</script>

</body>
</html>
